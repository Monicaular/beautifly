from django.apps import AppConfig


class BasketConfig(AppConfig):
    """Configuration for the Basket app within the project."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "basket"
